{namespace tree.templates}

/**
 * Get section template for tree of particular thread
 * @param threadId The name of the person.
 */
{template .getSectionForThread}
<section id="{$threadId}">
<h2>{$threadId}</h2>
</section>
{/template}